# WebAssembly Boilerplate

Install 3rd party dependencies:

```
npm install
```

Build debug version:

```
npm run build:debug
```

Build release version:

```
npm run build:release
```

Serve debug version:

```
npm run serve
```